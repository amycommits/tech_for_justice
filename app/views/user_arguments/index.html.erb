<% @video_url = "https://www.youtube.com/embed/sXakTGa1OIs?list=PLIoHkUByRQSbW3gHYYl9qe9YxOYybPxeB" %> 

<p id="notice"><%= notice %></p>

<h1>All User Arguments</h1>

<table>
  <thead>
    <tr>
      <th>Main reason</th>
      <th>Other reason</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_arguments.each do |user_argument| %>
      <tr>
       
        <td><%= user_argument.overall_reason %></td>
        <td><%= user_argument.user_change_details %></td>
        <td><%= link_to 'Show', user_argument %></td>
        <td><%= link_to 'Edit', edit_user_argument_path(user_argument) %></td>
        <td><%= link_to 'Destroy', user_argument, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h1>New User argument</h1>
<%= render 'form' %>
<br/>
<%= link_to 'Next: User Reference(s)', user_references_path, class: 'submit_button' %>
