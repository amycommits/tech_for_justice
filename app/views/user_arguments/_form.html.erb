<%= form_for(@user_argument, remote: true) do |f| %>
  <% if @user_argument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_argument.errors.count, "error") %> prohibited this user_argument from being saved:</h2>

      <ul>
      <% @user_argument.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :overall_reason %><br>
    <%= f.text_field :overall_reason %>
  </div>
  <div class="field">
    <%= f.label :user_change_details %><br>
    <%= f.text_field :user_change_details %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'submit_button'%>
  </div>
<% end %>
